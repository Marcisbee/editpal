{
  "name": "editpal",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/editpal.js",
  "scripts": {
    "dev": "esbuild src/main.tsx --bundle --outdir=www --watch --servedir=www --sourcemap=inline",
    "build": "esbuild src/editpal.tsx --bundle --outdir=dist --define:process.env.NODE_ENV=\\\"production\\\" --minify --legal-comments=none --format=esm --platform=browser --external:exome --external:react --external:react-dom --analyze=verbose --mangle-props=^_ --mangle-quoted --pure:console.log",
    "declarations": "tsc src/editpal.tsx --declaration --emitDeclarationOnly --outDir dist --jsx react-jsx"
  },
  "dependencies": {
    "exome": "1.4.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.19",
    "@types/react-dom": "^18.0.6",
    "esbuild": "^0.17.2",
    "typescript": "^4.6.4"
  },
  "files": [
    "dist"
  ]
}
